#include "calculator.h"
#include "calculator_lcd.h"

void calculator_task(void* vptr) {
  // initialization
  calculator_lcd_init();
  
  
  
  // temp, pass me the real struct
  
  for(;;) {
  }
}
